# Renomeie este arquivo para ".env" e preencha com seus dados reais.
# NUNCA suba o arquivo .env de verdade para o GitHub (coloque .env no .gitignore).

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=sua_senha_aqui
PGDATABASE=conecta_servicos

PORT=3000
